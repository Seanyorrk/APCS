package Square;

public class Squares {
    public static void main(String[] args) {
       //Square should store the element in an integer array of 5 elements
         int[] squares = new int[6];
           for (int i = 0; i < squares.length; i++) {
               squares[i] = i * i;

            //application then displays the value of each element in the array
            


    }
    for (int j = 0; j < squares.length; j++) {
                System.out.println(squares[j]);
            }
}

}